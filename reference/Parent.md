# Parent name of a node

xxx

## Usage

``` r
Parent(level, node = NULL)
```

## Arguments

- level:

  A string designing the type of entity/pipeline. Available values are:
  `peptide`, `protein`

- node:

  xxx

  \#' @examples Parent('protein', 'Missing') Parent('protein', 'Missing
  POV') Parent('protein', c('Missing POV', 'Missing MEC'))
  Parent('protein', c('Missing', 'Missing POV', 'Missing MEC'))

## Value

NA
